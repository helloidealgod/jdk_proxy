int *pointer;
int **pointer;
int ***pointer;
int a;
int b,c;
int d=0,g;
struct abc{
};

struct abc{
int a,b;
int c;
} ghj;

struct abc a[];
struct abc a[234];
struct abc ab[234][67];
void test(int p1,int p2);
void sub(int a,int b){
    char c;
    int *a;
    int o = (p1 + m) * k;
    int a = b * c / d % e;
    int o = p + i - 91;
    int i = p + b + 90 + u * t / q + w;
    int o = (p1 + m) * (k / u);
    int o = (p1 + m) != (k / u);

    c = 'k';

    if(a > 0){
        char cc;
    }
    for(i=0;i<10;i=i+1){
        c = add();
    }
    while(b <= 0){
        int k=0;
        k=add(a)+add1(a);
        k=add(a,b,c);
    }
    return;
    return 0;
    return a;
    return a+b;
    return a > 0;
    a = &c;
    a = *c;
    a = +c;
    a = -c;
    sum = var1 + var2;
    p = p0 + p1 * p2;

    a = a1 != a2;
}