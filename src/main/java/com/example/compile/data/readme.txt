#include <string>
#include "string"

#define string1 string2
typedef type string


变量声明：
	type * name [d] = {value,value}, [*] name [d] = {value,value};
	struct typename { } * name= {};

函数声明：
	type * functionname(type * name[] , ...);

函数定义
	type * functionname(type * name[] , ...){
		变量声明、初始化；
		变量赋值（常量、运算、函数返回值）

		if else
		for
		while
		do while
		switch case
	}

类型 变量名 初始化值 代码指令列表
结构体 名字 类型
入参列表
